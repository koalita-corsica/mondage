import { Link } from "gatsby";
import React from "react";
import Icon from "./icon";
import { cn } from "../lib/helpers";


const Main = ({ children }) => (
  
  <div>{children}
  </div>
  
);

export default Main;
